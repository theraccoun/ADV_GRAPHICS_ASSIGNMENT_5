README

MacCoun_Assignment5

To install, simply type:

ant debug   (from 

Then cd into the bin directory and type (from SDK's platform-tools directory if this isn't on your path):

(for a device) :   adb -d install <path-to-adk-debug>.adk  
(for emulator) :   adb install <path-to-adk-debug>.adk                 (after selecting your chosen emulator)



Watch and wait as the world is slowly built up into a complete globe.

Also, hit the button at the top to watch HYPNO-DOG in action!!!
